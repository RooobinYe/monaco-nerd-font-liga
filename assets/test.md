## Common
```
Arithmetics ++ -- /= || ||= (Remove &&)
Scope -> => :: __
Equality == === != =/= !==
Comparison <= >= <=>
Comments /* */ // ///
Escaped operations None
Bit operations << <<< <<= >> >>> >>= |= ^=
```

## Language-specific
```text
JavaScript ** === !== ?.
HTML </ <!-- </> --> /> (Remove www)
Markdown # ## ### #### ####
         - -- ---
         = == ===
Java <> <~>
C# ?. ??
Ruby .. ... <=> (Not support =~ !~)
Go :=
Swift ..<
Kotlin !! ?. ..
R :::
Scala =!= =:= ::: (Not support <:<)
Rust ..= (Not support ::<)
Clojure #{ #( #_ #_( #? #: .- ;; (Remove ~@)
Elixir <- -> |> <> (Not support #{})
Haskell =>> =<< >>= <<= (Not support =>= =<=)
        <$ <$> $> <+ <+> +> <* <*> *>
        <> <|> .= #= +++ *** :>:
F# <||| <|| <| |> ||> |||>
   <- [| |] ~- ~~ %%
Logic /\ \/ -| _|_ |- ||- |= ||=
Other :> :< >: <: (Not support ::> <:: >::)
      {| |} #[ ]# ::= #! #= 
```
